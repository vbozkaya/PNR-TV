<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playerSettingsPanel"
    android:layout_width="300dp"
    android:layout_height="match_parent"
    android:background="#F21A1A1A"
    android:padding="24dp"
    android:visibility="gone"
    android:elevation="30dp"
    android:nextFocusUp="@id/playerSettingsPanel"
    android:nextFocusDown="@id/playerSettingsPanel"
    android:nextFocusLeft="@id/playerSettingsPanel"
    android:nextFocusRight="@id/playerSettingsPanel">
    
    <!-- Başlık -->
    <TextView
        android:id="@+id/txt_subtitle_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/player_subtitles_title"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    
    <!-- Alt Yazılar RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_subtitle_tracks"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:minHeight="100dp"
        android:nestedScrollingEnabled="true"
        android:focusable="false"
        android:focusableInTouchMode="false"
        app:layout_constraintTop_toBottomOf="@id/txt_subtitle_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    
    <!-- Kaydet Butonu - Kaldırıldı, seçim anında uygulanıyor -->
    <Button
        android:id="@+id/btnSaveSettings"
        android:layout_width="200dp"
        android:layout_height="48dp"
        android:text="@string/save"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:background="@drawable/button_focus_background"
        android:visibility="gone"
        android:focusable="false"
        android:focusableInTouchMode="false"
        app:layout_constraintTop_toBottomOf="@id/recycler_subtitle_tracks"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />
    
</androidx.constraintlayout.widget.ConstraintLayout>
